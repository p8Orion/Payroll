# SQL Server (Produccion)

Esta carpeta deja lista la base para conexion a SQL Server en produccion.

## Siguiente paso

- Definir ORM (Prisma/TypeORM/Drizzle) con dos providers:
  - tests: in-memory.
  - prod: SQL Server.
- Versionar migraciones de entidades de dominio.
